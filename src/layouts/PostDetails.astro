---
import Layout from "@layouts/Layout.astro";
import Header from "@components/Header.astro";
import Footer from "@components/Footer.astro";
import Tag from "@components/Tag.astro";
import Datetime from "@components/Datetime";
// import LinkButton from "@components/LinkButton.astro";
import type { CollectionEntry } from "astro:content";
// import { getCollection, getDataEntryById } from "astro:content";
import { slugifyStr } from "@utils/slugify";

export interface Props {
  post: CollectionEntry<"blog">;
}
// const posts = await getCollection("blog", ({ data }) => !data.draft);

const { post } = Astro.props;
// const { id, slug } = post;

const { title, author, description, ogImage, date, tags } = post.data;

const { Content } = await post.render();

const ogUrl = new URL(ogImage ? ogImage : `${title}.png`, Astro.url.origin)
  .href;

// console.log({ post, id, slug })
// const curIndex = posts.sort((a,b) => new Date(a.data.date) - new Date(b.data.date)).map(p => p.slug).indexOf(slug);
// console.log({ curIndex });

// posts.map((dt) => {
//   console.log(dt.data)
// })
// console.log(posts.length)
// const prevPost = {
//   id: posts[curIndex - 1].data.postSlug,
//   index: curIndex - 1,
// }
// const nextPost = {
//   id: posts[curIndex + 1].data.postSlug,
//   index: curIndex + 1,
// }
---

<Layout title={title} author={author} description={description} ogImage={ogUrl}>
  <Header />
  <div class="mx-auto flex w-full max-w-3xl justify-start px-2">
    <button
      class="focus-outline mb-2 mt-8 flex hover:opacity-75"
      onclick="history.back()"
    >
      <svg xmlns="http://www.w3.org/2000/svg"
        ><path
          d="M13.293 6.293 7.586 12l5.707 5.707 1.414-1.414L10.414 12l4.293-4.293z"
        ></path>
      </svg><span>Go back</span>
    </button>
  </div>
  <main id="main-content">
    <h1 class="post-title">{title}</h1>
    <Datetime datetime={date} size="lg" className="my-2" />
    <article id="article" role="article" class="prose mx-auto mt-8 max-w-3xl">
      <Content />
    </article>

    <ul class="tags-container">
      {tags.map(tag => <Tag name={slugifyStr(tag)} />)}
    </ul>
    <!-- <LinkButton
          disabled={prevPost.index === 0}
          href={`/posts/${prevPost.id}`}
          className={`mr-4 select-none `}
          ariaLabel="Previous"
        >
          Prev
        </LinkButton>
        <LinkButton
          disabled={nextPost.index === posts.length}
          href={`/posts/${nextPost.id}`}
          className={`ml-4 select-none `}
          ariaLabel="Next"
        >
          Next
        </LinkButton> -->
  </main>
  <Footer />
</Layout>

<style>
  main {
    @apply mx-auto w-full max-w-3xl px-4 pb-12;
  }
  .post-title {
    @apply text-2xl font-medium text-skin-accent;
  }
  .tags-container {
    @apply my-8;
  }
</style>
