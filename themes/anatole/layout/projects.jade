extends partial/layout

block site_title
    != page.projects + " · " + config.title

block description
    - var desc = 'Projects: ' + page.projects + ' - ' + config.title;
    meta(name="description", content=desc)

block content
  .autopagerize_page_element: .content
    .archive.animated.fadeInDown
      ul.list-with-title
        include mixins
        +postList()
